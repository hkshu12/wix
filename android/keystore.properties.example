# Copy to keystore.properties and point at your release keystore.
# This repo ships android/wix-release.keystore for consistent release APK signing.
storeFile=wix-release.keystore
storePassword=wix-release-store
keyAlias=wix
keyPassword=wix-release-store
