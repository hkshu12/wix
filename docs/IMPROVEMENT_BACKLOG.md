# Wix 改进 Backlog

由每日 6:00（北京时间）自动化 Cloud Agent 维护。Agent 每次运行会先更新本文件，再实现 **一项** 高优先级条目。

## 已发现项

| 优先级 | 场景 / 需求 | 当前不足 | 建议方案 |
| --- | --- | --- | --- |
| P1 | 睡前自动停止 | 无睡眠定时，需手动暂停 | Studio 增加可选定时器（15/30/45/60 分钟或自定义），到时淡出并停止播放 |
| P1 | 快速恢复常用声景 | 每次需重新点选多轨 | 本地保存 1–3 个「预设」混音（轨道 + 音量/声像），一键加载 |
| P2 | 专注/冥想渐入 | 开始播放较突兀 | 播放开始时主音量 0→目标值 2–5 秒渐入 |
| P2 | Android 锁屏/后台 | 切应用或锁屏后可能暂停 | 调研 Capacitor 后台音频插件或 Media Session，保持播放 |
| P2 | 混音配置分享 | 无法分享当前组合 | 将 mixer 状态编码为 URL hash 或短 JSON，只读还原（可选） |
| P3 | 键盘与读屏 | 声卡网格以按钮为主但缺少快捷键 | 为播放/混音抽屉增加键盘快捷键与 `aria-live` 播放状态 |
| P3 | 导入失败反馈 | 大文件或损坏文件错误信息较弱 | 在 `importStatus` 中区分大小限制、解码失败、配额错误 |

## 本次选中项

_（等待首次每日 Agent 运行填写）_

## 历史已完成

| 日期 | 改进 | 链接 |
| --- | --- | --- |
| — | — | — |
